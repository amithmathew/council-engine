---
import "../styles/global.css";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content="Council Engine — A structured deliberation protocol across diverse AI models. Better judgment through multi-model proposals, critique, and resolution." />
    <meta property="og:title" content="Council" />
    <meta property="og:description" content="A structured deliberation protocol across diverse AI models." />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,600;1,6..72,400&display=block" as="style" />
    <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,600;1,6..72,400&display=block" rel="stylesheet" />
    <title>Council — Structured AI Deliberation</title>
  </head>
  <body>

    <!-- ====== Nav ====== -->
    <nav class="site-nav" id="nav">
      <div class="nav-inner wrap-lg">
        <a href="#" class="nav-logo">Council</a>
        <div class="nav-links">
          <a href="#problem">Problem</a>
          <a href="#protocol">Protocol</a>
          <a href="#science">Science</a>
          <a href="/architecture">Architecture</a>
          <a href="#start">Start</a>
        </div>
      </div>
    </nav>

    <!-- ====== 1. Hero ====== -->
    <section class="hero" id="hero">
      <div class="wrap-md hero-content">
        <h1>Council</h1>
        <p class="hero-subtitle">A structured deliberation protocol across diverse AI models</p>
        <p class="hero-body">
          Single-model outputs are fluent, fast, and often wrong in the same direction.
          Council runs a bounded deliberation across different models, processes critique
          explicitly, and returns a decision you can inspect. The point is not more text.
          It is better judgment.
        </p>
        <div class="hero-cta">
          <a href="#start" class="btn btn-primary">pip install council-engine</a>
          <a href="https://github.com/amithmathew/council-engine" class="btn btn-secondary" target="_blank" rel="noopener">View on GitHub</a>
        </div>
      </div>
    </section>

    <hr class="section-sep" />

    <!-- ====== 2. The Illusion of Consensus ====== -->
    <section class="problem" id="problem">
      <div class="wrap-sm">
        <p class="eyebrow">The problem</p>
        <h2>The illusion of consensus</h2>

        <p>
          A single answer can sound settled long before the underlying question is resolved.
        </p>

        <p>
          Most AI interfaces compress generation, critique, and conclusion into one response.
          That response may hide uncertainty, skip alternatives, or converge too early on a
          plausible story. Fluency makes that failure mode easy to miss.
        </p>

        <p class="callout">
          Different AI models have different biases and blind spots. Council puts those
          differences to work.
        </p>

        <p>
          Instead of asking one model to simulate a debate with itself, Council collects
          distinct proposals, forces explicit critique, and makes disagreement visible.
          You see where the options diverge, what objections survive scrutiny, and what
          still needs evidence.
        </p>

        <p>
          The goal is not more output. It is better judgment.
        </p>
      </div>
    </section>

    <hr class="section-sep" />

    <!-- ====== 3. A Bounded Deliberation Protocol ====== -->
    <section class="protocol" id="protocol">
      <div class="wrap-md">
        <p class="eyebrow">How it works</p>
        <h2>A bounded deliberation protocol</h2>
        <p class="section-lead">
          Council runs a structured, three-stage mechanism.
        </p>
      </div>

      <!-- Protocol diagram -->
      <div class="wrap-lg">
        <div class="protocol-stages">
          <div class="stage">
            <div class="stage-number">1</div>
            <h3>Proposals</h3>
            <p>
              Multiple models respond to the same task independently.
              Each produces a clear position, not a stream of partial thoughts.
            </p>
            <div class="stage-models">
              <span class="model-dot" style="background: var(--gemini);" title="Gemini"></span>
              <span class="model-dot" style="background: var(--chatgpt);" title="ChatGPT"></span>
              <span class="model-dot" style="background: #d4a574;" title="Claude"></span>
              <span class="model-dot" style="background: #88c0d0;" title="Ollama"></span>
            </div>
          </div>
          <div class="stage-connector" aria-hidden="true">
            <svg width="40" height="2"><line x1="0" y1="1" x2="40" y2="1" stroke="var(--line-strong)" stroke-width="1.5"/></svg>
          </div>
          <div class="stage">
            <div class="stage-number">2</div>
            <h3>Critique</h3>
            <p>
              Models review each other through a constrained vocabulary:
              <strong>challenge</strong>, <strong>alternative</strong>,
              <strong>refinement</strong>, and <strong>question</strong>.
            </p>
            <div class="critique-vocab">
              <span class="vocab-tag">challenge</span>
              <span class="vocab-tag">alternative</span>
              <span class="vocab-tag">refinement</span>
              <span class="vocab-tag">question</span>
            </div>
          </div>
          <div class="stage-connector" aria-hidden="true">
            <svg width="40" height="2"><line x1="0" y1="1" x2="40" y2="1" stroke="var(--line-strong)" stroke-width="1.5"/></svg>
          </div>
          <div class="stage">
            <div class="stage-number">3</div>
            <h3>Resolution</h3>
            <p>
              A final pass classifies the disagreement and produces one of four adaptive outcomes.
            </p>
            <div class="resolution-types">
              <span class="resolution-tag">recommendation</span>
              <span class="resolution-tag">alternatives</span>
              <span class="resolution-tag">question</span>
              <span class="resolution-tag">investigate</span>
            </div>
          </div>
        </div>
      </div>

      <!-- What the protocol produces -->
      <div class="wrap-sm protocol-outcomes">
        <p>
          The system does not force false agreement. When models agree for good reasons,
          you get a recommendation. When they disagree on real tradeoffs, you get alternatives.
          If the prompt is underspecified, you get a blocking question. If the answer depends on
          missing facts, you get an explicit investigation path.
        </p>
      </div>

      <!-- Deliberation Board -->
      <div class="wrap-lg">
        <div class="delib-board">
          <div class="delib-header">
            <span class="eyebrow">Real deliberation</span>
            <span class="delib-meta">2 models &middot; 3 stages &middot; 1 follow-up question</span>
          </div>
          <p class="delib-prompt">
            "We're a 4-person team running a customer-facing API. After 3 deploy incidents this month, the VP of Engineering wants us to add a staging environment. I think that'll slow us down and the real problem is our lack of observability. We ship 5-10 times a week and I don't want to lose that velocity. How should I make this case, or am I wrong?"
          </p>

          <!-- Proposals row -->
          <div class="delib-proposals">
            <div class="delib-card" style="--participant-color: var(--chatgpt);">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--chatgpt);"></span>
                <span class="card-model">chatgpt</span>
                <span class="card-stage">proposal</span>
              </div>
              <p class="card-title">Fix the failure modes first &mdash; add staging only if incidents are integration gaps prod-safe testing can't catch</p>
              <p class="card-body">
                Your incident pattern matters more than the generic best practice. A staging environment often catches less than leaders expect because it rarely matches real production traffic, data shape, or integrations. The politically smart argument is not &ldquo;staging is dumb&rdquo; &mdash; it is: fix the incident classes you actually had, with the cheapest controls that would have prevented them.
              </p>
            </div>
            <div class="delib-card" style="--participant-color: var(--gemini);">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--gemini);"></span>
                <span class="card-model">gemini</span>
                <span class="card-stage">proposal</span>
              </div>
              <p class="card-title">Reject staging &mdash; implement feature flags and automated rollbacks driven by observability</p>
              <p class="card-body">
                Staging environments inevitably drift from production, creating a false sense of security while bottlenecking a 4-person team into a rigid deployment queue. Progressive delivery solves the VP's underlying goal &mdash; eliminating customer-facing incidents &mdash; while preserving your 5&ndash;10x weekly velocity.
              </p>
            </div>
          </div>

          <!-- Critique row -->
          <div class="delib-critiques">
            <div class="delib-card critique-card" style="--participant-color: var(--chatgpt);">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--chatgpt);"></span>
                <span class="card-model">chatgpt</span>
                <span class="card-stage">challenge &rarr; gemini</span>
              </div>
              <p class="card-body">
                Gemini is overclaiming on feature flags. Flags do not prevent customer impact for schema changes, config errors, dependency/runtime issues, bad migrations, or deploy-time infrastructure mistakes &mdash; the exact classes staging can catch. The critical question is not &ldquo;staging vs flags&rdquo; but whether the 3 incidents were release-control failures or environment/integration failures.
              </p>
            </div>
          </div>

          <!-- Synthesis row: Question resolution -->
          <div class="delib-synthesis">
            <div class="delib-card synth-card synth-question">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--synth);"></span>
                <span class="card-model">synthesis</span>
                <span class="card-stage">question</span>
              </div>
              <p class="card-body">
                Were your recent deploy incidents caused by <strong>structural issues</strong> (config drift, missing env vars, schema mismatches) or <strong>emergent issues</strong> (production data edge cases, unexpected traffic, stateful third-party behaviors)? A staging environment primarily catches structural errors. Observability and progressive delivery primarily mitigate emergent errors.
              </p>
            </div>

            <!-- User answer -->
            <div class="delib-card user-card">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--user);"></span>
                <span class="card-model">you</span>
              </div>
              <p class="card-body">Not sure, but I believe they were structural.</p>
            </div>

            <!-- Re-resolution -->
            <div class="delib-card synth-card">
              <div class="card-identity">
                <span class="card-dot" style="background: var(--synth);"></span>
                <span class="card-model">synthesis</span>
                <span class="card-stage">investigate</span>
              </div>
              <p class="card-title">Don't push back on the VP yet &mdash; verify your hunch first</p>
              <p class="card-body">
                If the incidents were truly structural, your VP is right: feature flags and observability do not prevent structural deploy failures. But you're unsure. Pull the post-mortems, categorize each failure, and ask: would staging have realistically caught this without production-scale traffic?
              </p>
              <div class="synth-sections">
                <div class="synth-detail">
                  <span class="synth-label">provisional</span>
                  <span>Go to the VP: &ldquo;Give me 48 hours to map the exact failure modes of the last three incidents so we invest in the right safety net without killing our velocity.&rdquo;</span>
                </div>
                <div class="synth-detail">
                  <span class="synth-label">if structural</span>
                  <span>Build ephemeral preview environments per PR &mdash; automated pre-prod verification without the permanent staging tax.</span>
                </div>
                <div class="synth-detail">
                  <span class="synth-label">if emergent</span>
                  <span>Show the VP the data proving staging wouldn't have caught the bugs, and pitch progressive delivery: feature flags, canaries, and automated fast-rollbacks.</span>
                </div>
              </div>
            </div>
          </div>

          <p class="delib-caption">
            Real output from a Council session. The council identified a missing fact, asked a clarifying question, and re-resolved with the answer.
            <a href="/deliberation" class="delib-full-link">View full deliberation &rarr;</a>
          </p>
        </div>
      </div>
    </section>

    <hr class="section-sep" />

    <!-- ====== 4. The Science of Heterogeneity ====== -->
    <section class="science" id="science">
      <div class="wrap-sm">
        <p class="eyebrow">Research</p>
        <h2>The case for heterogeneity</h2>

        <p>
          Prompt variation helps. Persona variation can help. But they are not the same
          as model diversity.
        </p>

        <p>
          Research on multi-agent debate shows that performance improves when systems
          compare competing answers instead of extending a single chain of
          reasoning.<sup><a href="#ref-1">1</a></sup> More recent work suggests the gains
          are strongest when the agents are actually different. Same-model personas tend to
          converge on the same errors and hit diminishing
          returns.<sup><a href="#ref-2">2</a>,<a href="#ref-3">3</a></sup>
        </p>

        <p>
          If all participants share the same base model, the interaction adds style diversity
          without adding epistemic diversity. Heterogeneous setups are fundamentally better at
          surfacing conflicting assumptions and different failure
          modes.<sup><a href="#ref-3">3</a>,<a href="#ref-4">4</a></sup> Furthermore,
          persona prompting alone does not reliably improve factual
          performance.<sup><a href="#ref-5">5</a></sup>
        </p>

        <p>
          Council is built on that premise: use genuinely different models, structure their
          interaction, and make the resolution explicit.
        </p>
      </div>
    </section>

    <hr class="section-sep" />

    <!-- ====== 5. Built for Auditability and Local Control ====== -->
    <section class="architecture" id="architecture">
      <div class="wrap-md">
        <p class="eyebrow">Architecture</p>
        <h2>Built for auditability and local control</h2>
        <p class="section-lead">
          High-stakes use requires an inspectable system.
        </p>

        <div class="arch-grid">
          <div class="arch-card">
            <h3>Event Bus</h3>
            <p>
              Orchestration is decoupled from rendering. Deliberation events are emitted
              once and rendered wherever needed: terminal, board, logs, or downstream tooling.
            </p>
          </div>
          <div class="arch-card">
            <h3>Persistent Audit Trail</h3>
            <p>
              Each run is persisted in SQLite. Proposals, critiques, tool actions, and final
              resolutions are stored as a durable record. You can trace exactly how a
              conclusion was formed.
            </p>
          </div>
          <div class="arch-card">
            <h3>Sandboxed Operations</h3>
            <p>
              File access is handled through a specialized chair and secretary subsystem.
              Models do not receive ambient authority over the filesystem. Actions are
              requested, mediated, and recorded.
            </p>
          </div>
          <div class="arch-card">
            <h3>Workspace Integration</h3>
            <p>
              The system supports an interactive REPL and direct workspace integration,
              making it useful in real analysis workflows rather than isolated web interfaces.
            </p>
          </div>
        </div>

        <div class="arch-cta">
          <a href="/architecture" class="btn btn-secondary">Explore the architecture in depth</a>
        </div>
      </div>
    </section>

    <hr class="section-sep" />

    <!-- ====== 6. Quick Start & References ====== -->
    <section class="quickstart" id="start">
      <div class="wrap-sm">
        <p class="eyebrow">Get started</p>
        <h2>Quick start</h2>

        <p>Install Council.</p>
        <pre><code>pip install council-engine</code></pre>

        <p>Configure your providers. Supports Gemini, ChatGPT, Claude, and Ollama.</p>
        <pre><code>council init</code></pre>

        <p>Invoke your first council.</p>
        <pre><code>council</code></pre>

        <p class="quickstart-hint">
          Start with a question that benefits from real disagreement: a design decision,
          a tradeoff analysis, a risky refactor, a policy choice, or a research synthesis.
          Council works best when the cost of a shallow answer is high.
        </p>

        <div class="references" id="references">
          <h3>References</h3>
          <ol class="ref-list">
            <li id="ref-1">
              Du, Y. et al. 2023. <a href="https://arxiv.org/abs/2305.14325" target="_blank" rel="noopener"><em>Improving Factuality and Reasoning in Language Models through Multiagent Debate.</em></a>
            </li>
            <li id="ref-2">
              Yang, Y. et al. 2026. <a href="https://arxiv.org/abs/2602.03794" target="_blank" rel="noopener"><em>Understanding Agent Scaling in LLM-Based Multi-Agent Systems via Diversity.</em></a>
            </li>
            <li id="ref-3">
              Zhang, H. et al. 2025. <a href="https://arxiv.org/abs/2502.08788" target="_blank" rel="noopener"><em>If Multi-Agent Debate is the Answer, What is the Question?</em></a>
            </li>
            <li id="ref-4">
              Lau, G. et al. 2024. <a href="https://arxiv.org/abs/2412.15238" target="_blank" rel="noopener"><em>Dipper: Diversity in Prompts for Producing Large Language Model Ensembles in Reasoning Tasks.</em></a>
            </li>
            <li id="ref-5">
              Zheng, M. et al. 2023. <a href="https://arxiv.org/abs/2311.10054" target="_blank" rel="noopener"><em>When "A Helpful Assistant" Is Not Really Helpful: Personas in System Prompts Do Not Improve Performances of Large Language Models.</em></a>
            </li>
          </ol>
        </div>
      </div>
    </section>

    <!-- ====== Footer ====== -->
    <footer class="site-footer">
      <div class="wrap-md footer-inner">
        <span class="footer-logo">Council</span>
        <span class="footer-sep">&middot;</span>
        <span>An open-source AI deliberation engine</span>
        <span class="footer-sep">&middot;</span>
        <a href="https://github.com/amithmathew/council-engine" target="_blank" rel="noopener">GitHub</a>
      </div>
    </footer>

  </body>
</html>

<style>
  /* ====== Nav ====== */
  .site-nav {
    position: sticky;
    top: 0;
    z-index: 100;
    height: var(--nav-height);
    background: rgba(11, 13, 16, 0.72);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
  }

  .nav-logo {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text);
    text-decoration: none;
    letter-spacing: -0.01em;
  }

  .nav-links {
    display: flex;
    gap: 28px;
  }

  .nav-links a {
    font-family: var(--font-code);
    font-size: 13px;
    font-weight: 500;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.15s;
  }

  .nav-links a:hover {
    color: var(--text);
    text-decoration: none;
  }

  /* ====== Hero ====== */
  .hero {
    padding-top: 128px;
    padding-bottom: 96px;
  }

  .hero-content {
    text-align: left;
  }

  .hero h1 {
    font-size: var(--fs-hero);
    margin-bottom: 16px;
    letter-spacing: -0.03em;
  }

  .hero-subtitle {
    font-family: var(--font-heading);
    font-size: var(--fs-h2);
    font-weight: 400;
    font-style: italic;
    color: var(--text-soft);
    max-width: 60ch;
    margin-bottom: 32px;
    line-height: 1.2;
  }

  .hero-body {
    font-size: var(--fs-body-lg);
    color: var(--text-soft);
    max-width: 60ch;
    margin-bottom: 40px;
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  /* ====== Problem ====== */
  .callout {
    font-family: var(--font-heading);
    font-size: var(--fs-h3);
    font-weight: 400;
    font-style: italic;
    color: var(--text);
    max-width: 56ch;
    margin-block: 1.5em;
    line-height: 1.35;
  }

  /* ====== Protocol Stages ====== */
  .section-lead {
    font-size: var(--fs-body-lg);
    color: var(--text-soft);
    max-width: 60ch;
    margin-bottom: 48px;
  }

  .protocol-stages {
    display: flex;
    align-items: flex-start;
    gap: 0;
    margin-bottom: 48px;
  }

  .stage {
    flex: 1;
    background: var(--bg-panel);
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 28px 24px;
  }

  .stage-number {
    font-family: var(--font-code);
    font-size: var(--fs-label);
    font-weight: 600;
    color: var(--text-muted);
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--line-strong);
    border-radius: 50%;
    margin-bottom: 16px;
  }

  .stage h3 {
    font-family: var(--font-code);
    font-size: var(--fs-small);
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 12px;
  }

  .stage p {
    font-size: var(--fs-small);
    color: var(--text-soft);
    line-height: 1.55;
  }

  .stage-connector {
    display: flex;
    align-items: center;
    padding-top: 48px;
    flex-shrink: 0;
  }

  .stage-models {
    display: flex;
    gap: 6px;
    margin-top: 16px;
  }

  .model-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    opacity: 0.8;
  }

  .critique-vocab, .resolution-types {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 16px;
  }

  .vocab-tag, .resolution-tag {
    font-family: var(--font-code);
    font-size: 11px;
    color: var(--text-muted);
    padding: 3px 10px;
    border: 1px solid var(--line);
    border-radius: 20px;
  }

  .resolution-tag {
    border-color: rgba(199, 121, 255, 0.2);
    color: var(--synth);
  }

  .protocol-outcomes {
    margin-bottom: 56px;
  }

  .protocol-outcomes p {
    color: var(--text-soft);
  }

  /* ====== Deliberation Board ====== */
  .delib-board {
    background: linear-gradient(180deg, #10141A 0%, #0C1015 100%);
    border: 1px solid var(--line-strong);
    border-radius: 20px;
    padding: 32px;
    margin-bottom: 16px;
  }

  .delib-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 28px;
  }

  .delib-header .eyebrow {
    margin-bottom: 0;
  }

  .delib-meta {
    font-family: var(--font-code);
    font-size: var(--fs-label);
    color: var(--text-faint);
  }

  .delib-prompt {
    font-family: var(--font-heading);
    font-size: var(--fs-small);
    font-style: italic;
    color: var(--text-muted);
    line-height: 1.55;
    max-width: none;
    margin-bottom: 24px;
    padding: 16px 20px;
    background: rgba(255, 255, 255, 0.02);
    border-radius: 10px;
    border: 1px solid var(--line);
  }

  .delib-proposals {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 16px;
  }

  .delib-critiques {
    margin-bottom: 16px;
  }

  .delib-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 20px;
    border-top: 3px solid var(--participant-color, var(--line));
  }

  .critique-card {
    border-top: none;
    border-left: 3px solid var(--participant-color, var(--line));
  }

  .card-identity {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 12px;
  }

  .card-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .card-model {
    font-family: var(--font-code);
    font-size: 13px;
    font-weight: 500;
    color: var(--text-soft);
  }

  .card-stage {
    font-family: var(--font-code);
    font-size: 11px;
    color: var(--text-faint);
    margin-left: auto;
  }

  .card-title {
    font-family: var(--font-body);
    font-size: var(--fs-small);
    font-weight: 600;
    color: var(--text);
    margin-bottom: 8px;
    max-width: none;
  }

  .card-body {
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.55;
    max-width: none;
    margin-bottom: 0;
  }

  /* Synthesis card */
  .synth-card {
    border-top: 3px solid var(--synth);
    background: linear-gradient(180deg, rgba(199, 121, 255, 0.04), rgba(255, 255, 255, 0.02));
    border-color: rgba(199, 121, 255, 0.15);
    border-top-color: var(--synth);
  }

  .synth-question {
    margin-bottom: 12px;
  }

  /* User answer card */
  .user-card {
    border-top: 3px solid var(--user);
    margin-bottom: 12px;
  }

  .synth-sections {
    margin-top: 16px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .synth-detail {
    display: flex;
    gap: 12px;
    font-size: 13px;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .synth-label {
    font-family: var(--font-code);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--synth);
    flex-shrink: 0;
    padding-top: 1px;
  }

  .delib-caption {
    font-size: var(--fs-small);
    color: var(--text-faint);
    text-align: center;
    margin-top: 20px;
    font-style: italic;
    max-width: none;
  }

  .delib-full-link {
    display: inline-block;
    margin-top: 6px;
    font-style: normal;
    font-family: var(--font-code);
    font-size: 13px;
    color: var(--accent);
  }

  .delib-full-link:hover {
    color: var(--accent-hover);
  }

  /* ====== Architecture Grid ====== */
  .arch-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-top: 40px;
  }

  .arch-card {
    background: var(--bg-panel);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 28px 24px;
  }

  .arch-card h3 {
    font-family: var(--font-code);
    font-size: var(--fs-small);
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--text);
    margin-bottom: 10px;
  }

  .arch-card p {
    font-size: var(--fs-small);
    color: var(--text-soft);
    line-height: 1.6;
    max-width: none;
  }

  .arch-cta {
    margin-top: 32px;
  }

  /* ====== Quick Start ====== */
  .quickstart-hint {
    color: var(--text-muted);
    font-size: var(--fs-small);
    max-width: 60ch;
    margin-top: 24px;
  }

  .references {
    margin-top: 64px;
    padding-top: 40px;
    border-top: 1px solid var(--line);
  }

  .references h3 {
    font-family: var(--font-code);
    font-size: var(--fs-small);
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 20px;
  }

  .ref-list {
    list-style: none;
    counter-reset: ref;
    padding: 0;
  }

  .ref-list li {
    counter-increment: ref;
    font-size: var(--fs-small);
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 10px;
    padding-left: 24px;
    position: relative;
  }

  .ref-list li::before {
    content: "[" counter(ref) "]";
    position: absolute;
    left: 0;
    font-family: var(--font-code);
    font-size: 12px;
    color: var(--text-faint);
  }

  .ref-list em {
    color: var(--text-soft);
  }

  /* ====== Footer ====== */
  .site-footer {
    padding: 40px 0;
    border-top: 1px solid var(--line);
  }

  .footer-inner {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: var(--fs-small);
    color: var(--text-faint);
    flex-wrap: wrap;
  }

  .footer-logo {
    font-family: var(--font-heading);
    font-weight: 600;
    color: var(--text-muted);
  }

  .footer-sep {
    color: var(--line-strong);
  }

  /* ====== Responsive ====== */
  @media (max-width: 900px) {
    .hero {
      padding-top: 96px;
      padding-bottom: 64px;
    }

    .protocol-stages {
      flex-direction: column;
      gap: 12px;
    }

    .stage-connector {
      display: none;
    }

    .delib-proposals {
      grid-template-columns: 1fr;
    }

    .arch-grid {
      grid-template-columns: 1fr;
    }

    .nav-links {
      gap: 16px;
    }
  }

  @media (max-width: 640px) {
    .nav-links {
      display: none;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 10vw, 3.5rem);
    }

    .delib-board {
      padding: 20px;
      border-radius: 16px;
    }
  }
</style>
